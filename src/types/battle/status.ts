export interface UnifiedStatusItem {
  id: string;
  title: string;
  description: string;
  icon?: string;
  type?: string;
}

export interface AdminStatConfigItem {
  key: string;
  label: string;
}
